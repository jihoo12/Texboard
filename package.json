{
  "name": "tex-board",
  "version": "1.0.0",
  "description": "TeX Board — a visual LaTeX node editor",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^8.0.14"
  }
}
